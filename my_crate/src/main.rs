fn main() {
    println!("Hello, world!");
}

// when we write "cargo build" we compile the program with dev profile
// it is unoptimised and has debug info


// to compile the code with release profile we write "cargo build --release"
// it is optimised
